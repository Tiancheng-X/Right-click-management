# CONTEXT — 术语表

右键菜单管理器的统一语言。只收术语与语义，不收实现细节。

## 菜单条目（Entry）

右键菜单中呈现为一条可操作项的**逻辑单元**。一个条目可挂多个挂载点；对条目的操作作用于其全部挂载点。这是列表与详情页的唯一主角。

## 挂载点（Mount）

条目接入系统的位置（如「所有文件」「文件夹」「桌面背景」「驱动器」）。是条目的属性，不是独立条目。

## 适用场景（Scene）

用户视角的两大分类：**文件右键**（文件、文件夹、快捷方式上的菜单）与**桌面右键**（桌面及文件夹背景空白处的菜单）。由挂载点推导，导航与卡片标签均以此呈现，不再暴露挂载点原名。

## 条目类型（Kind）

静态动词 / shellex 处理器 / 打包 (MSIX) / 系统内置 / 自定义。类型决定条目能做什么操作（见 启用/禁用、删除）。

## 待生效（Pending Restart）

写操作完成后、资源管理器重启之前的中间状态。程序以不打断的 toast 提示，并在侧栏常驻「有改动待生效」徽章，提供一键重启；用户不重启也不被追迫。

## 来源（Source）

推导出的归属应用（如 7-Zip、Code）。推导规则：静态动词取命令行中首个可执行文件名；shellex 解析 CLSID 对应 DLL 的所在目录名，失败回退挂接键名。标注在卡片上，仅用于展示，不参与操作语义。

## 启用 / 禁用（Enable / Disable）

可逆的可见性切换，**不删除任何原有数据**。手段因类型而定（逐类定案见「核心域模型与操作语义」工单决议）。禁用后可一键恢复。

## 删除（Delete）

**断开挂接**：移除条目在菜单里的接入点，不触碰共享的系统注册本体。删除前强制自动快照；撤销不靠回收站，靠快照历史还原。

## 快照（Snapshot）

改动发生前对受影响数据的自动留档，带时间点，可在快照历史中浏览与还原。每条快照对应一次操作，保存受影响数据的**改动前完整状态**（前像）。另有**手动快照点**：用户主动拍下的当前全部受管数据状态；手动快照点与删除类快照永不自动清理。

## 还原（Restore）

把整条快照的前像写回系统，回到该操作之前的状态。还原前自动留档（还原本身可撤销）；写回是权威操作，期间被外部改动过的键在结果中如实报告。v1 只支持整条还原，不支持键级挑选。

## 可疑残留（Suspicious）

满足确定性失效特征的条目（命令指向的文件不存在；或指向的组件标识无效/未注册）。只标注 ⚠ 与建议，不自动处理。

## 菜单层（Menu Layer）

Windows 11 的双层菜单结构：**新版菜单**（默认折叠层）与**经典菜单**（「显示更多选项」层）。shellex 条目仅出现在经典层。条目详情标注所属层，筛选器不设此维度。

## 自定义条目（Custom Entry）

本程序创建并拥有的静态动词条目。可随时编辑与删除；**他人的条目只可禁用/删除，不可编辑**。

## 系统内置（Built-in）

新版菜单顶层的系统自带命令。**不可操作**（只读展示）。

## 一键切回经典菜单（Classic Toggle）

全局开关：把整个右键菜单切回 Win10 式完整经典菜单。属非官方手段，系统更新后可能失效，程序须提供失效兜底。
